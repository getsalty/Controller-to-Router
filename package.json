{
  "name": "controller-to-router",
  "description": "C#, Entity Framework, Controller - converted to - TypeScript, Prisma, TRPC Router",
  "version": "0.0.1",
  "author": "Garrett Long",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/getsalty/Controller-to-Router"
  },
  "type": "module",
  "scripts": {
    "dev": "deno run --allow-read --allow-write --allow-env --watch --allow-sys src/main.ts dev",
    "start": "deno run --allow-read --allow-write --allow-env --allow-sys src/main.ts",
    "test": "deno test --allow-read --allow-write --watch --import-map=./tests/unit.test.importmap.json  tests/ "
  }
}
